<configuration name="acl.conf" description="Network Lists">
  <network-lists>
    <!--
      [设计决策 2026-09-08] SIP profile(internal 5060 / external 5080) 默认对公网放开，
      FS 层不再做来源 IP 白名单。访问控制下沉到两层：
        1) 服务器安全策略(防火墙/安全组) 在网络边缘按端口放行真实对端；
        2) 网关侧 _access_point 按来源 IP 做业务级授权(未命中即 603 no_access_point)。
      trusted_peers / lan 等 FS 层 ACL 仅保留给 ESL(event_socket) 控制面使用。
    -->
    <!--
	 These ACL's are automatically created on startup.

	 rfc1918.auto  - RFC1918 Space
	 nat.auto      - RFC1918 Excluding your local lan.
	 localnet.auto - ACL for your local lan.
	 loopback.auto - ACL for your local lan.
    -->


    <list name="lan" default="allow">
      <node type="deny" cidr="192.168.42.0/24"/>
      <node type="allow" cidr="192.168.42.42/32"/>
    </list>

    <!--
	This will traverse the directory adding all users
	with the cidr= tag to this ACL, when this ACL matches
	the users variables and params apply as if they
	digest authenticated.
    -->
    <list name="domains" default="deny">
      <!-- domain= is special it scans the domain from the directory to build the ACL -->
      <node type="allow" domain="$${domain}"/>
      <!-- use cidr= if you wish to allow ip ranges to this domains acl. -->
      <!-- <node type="allow" cidr="192.168.0.0/24"/> -->
    </list>

  </network-lists>
</configuration>
