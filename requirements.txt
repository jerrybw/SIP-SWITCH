# SIP Switch Gateway — Python 依赖（钉版本，便于可复现部署）
# 安装：在仓库根执行 `pip install -r requirements.txt`
# 运行时 Python 建议 3.12（部署 venv 为 3.12）；FastAPI 0.141 需 >=3.9。

fastapi==0.141.1
uvicorn[standard]==0.52.4          # [standard] 额外带入 watchfiles/uvloop/httptools/websockets（热重载/性能）
sqlalchemy==2.0.52
PyMySQL==1.2.0                     # MySQL 驱动（mysql+pymysql://...）
PyYAML==6.0.3                     # 读取 config_settings.yaml
Jinja2==3.1.6                     # 管理端页面模板
python-multipart==0.0.32          # FastAPI 解析 form（/fs/directory POST、登录）

# FreeSWITCH ESL 客户端依赖（代码中 `from ESL import ESLconnection`）。
# 注意：部署 venv 中该包以「未纳入 pip 元数据」的方式存在（pip show/python -l 均查不到版本），
# 因此此处不钉版本。首次部署请执行 `pip install python-esl` 后补钉具体版本。
# python-esl  —— 不在 PyPI。请从 FreeSWITCH 源码安装其 Python 绑定（位于 libs/esl/，make 后 `pip install ./libs/esl`），
# 或在已装 FS 的机器上 `pip install python-esl`（FS 提供的包）。详见 README §4.2。
